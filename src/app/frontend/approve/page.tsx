import ComingSoonPage from "@/src/components/ComingSoonPage";

export default function ApprovePage() {
  return <ComingSoonPage eyebrow="Payment approval" title="Payment approval is not connected yet." description="This route will display the merchant, amount, token, network, destination, and expiry before asking the customer to approve." />;
}
