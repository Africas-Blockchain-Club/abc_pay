import ComingSoonPage from "@/src/components/ComingSoonPage";

export default function SignInPage() {
  return <ComingSoonPage eyebrow="Returning customers" title="Sign in is not available yet." description="Customer account creation is available now. Returning-user authentication will be added as a separate flow." />;
}
